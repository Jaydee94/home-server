---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: homepage-credentials
  namespace: homepage
spec:
  encryptedData:
    HOMEPAGE_VAR_GRAFANA_PASSWORD: "PLACEHOLDER_SEAL_WITH_KUBESEAL"
    HOMEPAGE_VAR_ARGOCD_TOKEN: "PLACEHOLDER_SEAL_WITH_KUBESEAL"
  template:
    metadata:
      name: homepage-credentials
      namespace: homepage
    type: Opaque
