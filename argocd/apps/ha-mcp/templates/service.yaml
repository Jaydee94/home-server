apiVersion: v1
kind: Service
metadata:
  name: {{ include "ha-mcp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ha-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ha-mcp.selectorLabels" . | nindent 4 }}
