{{- if and .Values.sealedSecret.encryptedSessionSecret .Values.sealedSecret.encryptedAdminPasswordHash -}}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.sealedSecret.secretName }}
  namespace: {{ .Release.Namespace }}
spec:
  encryptedData:
    sessionSecret: {{ .Values.sealedSecret.encryptedSessionSecret | quote }}
    adminPasswordHash: {{ .Values.sealedSecret.encryptedAdminPasswordHash | quote }}
  template:
    metadata:
      name: {{ .Values.sealedSecret.secretName }}
      namespace: {{ .Release.Namespace }}
    type: Opaque
{{- end }}
