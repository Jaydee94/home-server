---
# Handlers for paperless
- name: Restart paperless compose
  become: true
  ansible.builtin.shell: |
    docker compose pull && docker compose up -d
  args:
    chdir: "{{ paperless_base_dir }}"
  changed_when: true
